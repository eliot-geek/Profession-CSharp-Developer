namespace _19Loops
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // In C# there are three+one cyclic constructs (operator). 
            // They are used to do a certain sequence of operations several times.
            // loop is a kind of control construction, designed to organize multiple executions of a set of instructions.
            // A block of instructions to be executed is called a loop body.
            // a single execution of a set of instructions is called an iteration of a cycle
        }
    }
}