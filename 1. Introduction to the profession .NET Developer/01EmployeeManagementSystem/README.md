Program
Creating your own library
The main state unit of the application is the class (Program)
Creating a database of some employees
Printing to the console of all employees
Dismissal of all employees with some name
Dismissal of employees whose salary exceeds some salary

Worker class
A class describing the Worker model
Name, surname, age and salary of each employee
A constructor that allows you to assign a value to the corresponding fields of the employee
Organization of the output of information about the employee

Repository class
Organization of data storage and generation (Repository)
Database of names
Database of surnames
Pseudorandom Number Generator
A static constructor that "stores" data about firstNames and lastNames databases
Placing a random number generator in memory
Placing names in the database
Placing surnames in the database
An employee database that stores the first name, last name, age, and salaries of each employee
Constructor that populates the Workers database
Allocation of memory for storing the Workers database
Filling in the Workers database. Executed Count times
Add a new employee to the Workers database
select a random name from the names database
select a random name from the surnames database
Generating a random age in the range 
Generating a random salary in the range
Method of output of the Workers database to the console
We print all employees to the console
A method that dismisses employees with a salary greater than the specified one
A method that dismisses employees with a given name


HOMEWORK


// Difficulty level: Simple
// Task 1. Redo the program so that before the first wave of layoffs in the department there were no more than 20 employees


// Difficulty level: Medium difficulty
// * Task 2. Create a department of 40 employees and implement several layoffs, according to the results of which no more than 30 employees should remain in the department


// Difficulty level: Difficult
// ** Task 3. Create a department of 50 employees and implement the dismissal of employees whose salary exceeds 30,000 $