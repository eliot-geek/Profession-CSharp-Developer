using System.Diagnostics.Metrics;
using System.Xml.Linq;

namespace ArraysCourses
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // 4.1 One-Dimensional Arrays: Initialization, Definitions, Use, Assignment

            // An array is a data structure that stores a set of values(array elements) identified by an index or set of indices.
            // The array data type has a uniform data type. To declare an array, a set of square brackets is added to the data type.
            // This will indicate that you plan to create an array. After that, the array identifier(name) is specified.
            // An array can be declared with any data type, and all of its elements will have that type.
        }
    }
}